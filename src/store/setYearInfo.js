import { defineStore } from 'pinia';

export const useYearInfoStore = defineStore('yearInfo', {
  state: () => ({
    setYear: '',
    setFg: '',
    setLocCh: '',
  }),
  getters: {},
  actions: {},
});
